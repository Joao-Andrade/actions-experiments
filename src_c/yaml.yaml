kind: Application
metadata:
  name: app_c
version: 1.0.0
spec:
  source:
    helm:
      values: |
        image:
          tag: "1.4.0"
  sources:
    - repoURL: a
      chart: backend
      version: '1'
    - repoURL: a
      chart: frontend
      version: '1'